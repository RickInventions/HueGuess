info:
  name: Competitive Stats
  type: http
  seq: 9

http:
  method: GET
  url: "{{BASE_URL}}competitive/stats"
  auth:
    type: bearer
    token: "{{USER_1}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
