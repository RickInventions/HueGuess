info:
  name: Difficulty Check
  type: http
  seq: 12

http:
  method: GET
  url: "{{BASE_URL}}stats/difficulty-check"
  auth:
    type: bearer
    token: "{{USER_1}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
