info:
  name: Email Verif
  type: http
  seq: 4

http:
  method: POST
  url: "{{BASE_URL}}auth/verify-email"
  body:
    type: json
    data: |-
      {
        "email": "flashfit121@gmail.com",
        "code": "430654"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
