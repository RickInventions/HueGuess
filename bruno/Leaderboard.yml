info:
  name: Leaderboard
  type: http
  seq: 13

http:
  method: GET
  url: "{{BASE_URL}}leaderboard/top-players"
  auth:
    type: bearer
    token: "{{USER_1}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
