info:
  name: Login
  type: http
  seq: 5

http:
  method: POST
  url: "{{BASE_URL}}auth/login"
  body:
    type: json
    data: |-
      {
        "email": "flashfit121@gmail.com",
        "password": "Password121"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
