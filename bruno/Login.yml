info:
  name: Login
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}auth/login"
  body:
    type: json
    data: |-
      {
        "username": "BobbyDee",
        "email": "flashfit121@gmail.com",
        "password": "Password121"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
