info:
  name: Profile
  type: http
  seq: 8

http:
  method: GET
  url: "{{BASE_URL}}auth/me"
  auth:
    type: bearer
    token: "{{USER_1}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
