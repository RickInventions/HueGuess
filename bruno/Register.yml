info:
  name: Register
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}auth/register"
  body:
    type: json
    data: |-
      {
        "username": "testuser",
        "email": "{{third_email}}",
        "password": "Passwordd121"
      }
  auth: inherit

runtime:
  variables:
    - name: third_email
      value: flashfit121@gmail.com

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
