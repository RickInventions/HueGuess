info:
  name: Start
  type: http
  seq: 6

http:
  method: POST
  url: "{{BASE_URL}}round/start"
  body:
    type: json
    data: |-
      {
        "mode": "casual",
        "difficulty": "medium"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
