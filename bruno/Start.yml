info:
  name: Start
  type: http
  seq: 6

http:
  method: POST
  url: "{{BASE_URL}}game/round?mode=competitive&difficulty=medium"
  params:
    - name: mode
      value: competitive
      type: query
    - name: difficulty
      value: medium
      type: query
  auth:
    type: bearer
    token: "{{USER_1}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
