info:
  name: Start
  type: http
  seq: 4

http:
  method: POST
  url: "{{BASE_URL}}round/start"
  body:
    type: json
    data: |-
      {
        "mode": "casual",
        "difficulty": "medium"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
