info:
  name: Submit
  type: http
  seq: 7

http:
  method: POST
  url: "{{BASE_URL}}game/submit"
  body:
    type: json
    data: |-
      {
        "roundId": "{{ROUND_ID}}",
        "userColor":{
          "h":91,
          "s":91,
          "l":62
        }
      }
  auth:
    type: bearer
    token: "{{USER_1}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
