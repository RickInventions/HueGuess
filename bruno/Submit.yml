info:
  name: Submit
  type: http
  seq: 5

http:
  method: POST
  url: "{{BASE_URL}}round/submit"
  body:
    type: json
    data: |-
      {
        "roundId": "{{ROUND_ID}}",
            "h": 49,
            "s": 61,
            "l": 42
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
